import React from 'react'
import '../styles/Header.css'

const Header = () => {
    return (
        <header>
            <h1>Weather App</h1>
            <p>Check weather for desired city</p>
        </header>
    )
}

export default Header